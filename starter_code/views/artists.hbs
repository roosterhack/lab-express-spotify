<div class="artist">
  <ul>
    {{#each artists}}
    <li>
      <h3>{{ this.name}}</h3>
      <img src={{ this.images.[0].url}} alt={{this.name}}>
      <button><a href="/albums/{{this.id}}">View albums</a></button>
    </li>
    {{/each}}
  </ul>
</div>